{
  "mcpServers": {
    "inferlane": {
      "command": "npx",
      "args": ["-y", "@inferlane/mcp-server@^1.2.0"],
      "env": {
        "INFERLANE_API_KEY": "${user_config.api_key}"
      }
    }
  }
}
